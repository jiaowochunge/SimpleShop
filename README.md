### A simple e-commercial shop, only sell one good, only wxpay allowed

# How to generate project
* mvn archetype:generate -DgroupId=ai.bell -DartifactId=SimpleShop -DarchetypeArtifactId=maven-archetype-quickstart
* add dependencies
